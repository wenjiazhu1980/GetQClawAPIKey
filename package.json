{
  "name": "get-qclaw-api-key",
  "version": "1.0.0",
  "private": true,
  "description": "CLI tool to extract apiKey from a logged-in local QClaw installation",
  "scripts": {
    "start": "node scripts/get-key.mjs",
    "key": "node scripts/get-key.mjs",
    "models": "node scripts/models.mjs",
    "balance": "node scripts/balance.mjs",
    "test": "node --test",
    "bundle": "node scripts/bundle.mjs",
    "cli": "node scripts/cli.mjs",
    "build:win": "npm run bundle && pkg dist/cli.cjs --targets node18-win-x64 --output dist/get-qclaw.exe"
  },
  "devDependencies": {
    "@yao-pkg/pkg": "^5.16.0",
    "esbuild": "^0.24.0"
  }
}
